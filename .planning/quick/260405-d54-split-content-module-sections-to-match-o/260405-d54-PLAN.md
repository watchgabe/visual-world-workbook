---
type: quick
description: Split content module sections to match old HTML 1:1 (12 sections instead of 8)
files_modified:
  - src/components/sections/content/sustainability.tsx
  - src/components/sections/content/batching.tsx
  - src/components/sections/content/content-system.tsx
  - src/components/sections/content/starter-kit.tsx
  - src/components/sections/content/trust-and-money.tsx
  - src/components/sections/content/idea-generation.tsx
  - src/components/sections/content/storytelling.tsx
  - src/components/sections/content/overview.tsx
  - src/lib/modules.ts
  - src/app/(app)/modules/[slug]/[section]/page.tsx
---

<objective>
Split 3 combined content module section components into their proper 1:1 mapping with the old HTML's 10 parts. Currently sustainability.tsx bundles Parts 2+3, content-system.tsx bundles Parts 5+9, and trust-and-money.tsx bundles Parts 6+7+8. After this task, there will be 12 section components (overview + 10 parts + blueprint) each matching exactly one old HTML section.

Purpose: Content sections must match old app 1:1 per CLAUDE.md. The current combined sections make navigation confusing (e.g., "Batching System" link points to sustainability slug) and break the 10-part structure shown in the overview.
Output: 12 individual section components, updated MODULE_SECTIONS in modules.ts, updated SECTION_REGISTRY, updated overview roadmap slugs.
</objective>

<context>
@old/modules/content.html — Source of truth for all section content, titles, and field labels
@src/components/sections/content/ — All current content section components
@src/lib/modules.ts — MODULE_SECTIONS field definitions for content module (lines 194-300)
@src/app/(app)/modules/[slug]/[section]/page.tsx — SECTION_REGISTRY dynamic imports (lines 56-63)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split 3 combined components into 7 individual files (3 existing trimmed + 4 new)</name>
  <files>
    src/components/sections/content/sustainability.tsx
    src/components/sections/content/batching.tsx
    src/components/sections/content/content-system.tsx
    src/components/sections/content/starter-kit.tsx
    src/components/sections/content/trust-and-money.tsx
    src/components/sections/content/idea-generation.tsx
    src/components/sections/content/storytelling.tsx
  </files>
  <action>
    Reference old/modules/content.html as the source of truth for which content belongs in each section.

    **sustainability.tsx (SECTION_INDEX=2) — trim to Part 2 only:**
    - Keep only Workshop 2 content (Sustainable Foundation + Platform Strategy). This is roughly lines 1-457 of the current file.
    - Remove everything from Workshop 3 (Batching System) onward — that content moves to batching.tsx.
    - The SectionWrapper props stay the same (moduleSlug="content", sectionIndex=2).
    - Fields used: ct_sustain_* keys only (no ct_batch_* keys).

    **batching.tsx (SECTION_INDEX=3) — NEW, extract from sustainability.tsx:**
    - Create new component with the Workshop 3 content extracted from sustainability.tsx (roughly lines 458-678).
    - Use the same component patterns: import SectionWrapper, WorkshopInput/WorkshopTextarea, useAutoSave, MODULE_SECTIONS, etc.
    - SECTION_INDEX=3, MODULE_SLUG='content', slug will be 'batching'.
    - Fields used: ct_batch_film_day, ct_batch_count, ct_batch_setup, ct_batch_commit.
    - Content must match old HTML Part 3 exactly.

    **content-system.tsx (SECTION_INDEX=5) — trim to Part 5 (Waterfall) only:**
    - Keep only Workshop 5 content (The Waterfall Content System). This is roughly lines 1-425 of the current file.
    - Remove Workshop 9 (Starter Kit) content — that moves to starter-kit.tsx.
    - SECTION_INDEX changes from 4 to 5.

    **starter-kit.tsx (SECTION_INDEX=9) — NEW, extract from content-system.tsx:**
    - Create new component with Workshop 9 content extracted from content-system.tsx (roughly lines 426-610).
    - SECTION_INDEX=9, slug='starter-kit'.
    - This is educational-only content (no form fields), matching the old HTML Part 9.

    **trust-and-money.tsx (SECTION_INDEX=6) — trim to Part 6 only:**
    - Keep only Workshop 6 content (Trust & Money / Offer Stack). This is roughly lines 1-356 of the current file.
    - Remove Workshop 7 (Idea Generation) and Workshop 8 (Storytelling) — they move to their own files.
    - SECTION_INDEX changes from 5 to 6.
    - Fields used: ct_tm_* keys only.

    **idea-generation.tsx (SECTION_INDEX=7) — NEW, extract from trust-and-money.tsx:**
    - Create new component with Workshop 7 content extracted from trust-and-money.tsx (roughly lines 357-577).
    - SECTION_INDEX=7, slug='idea-generation'.
    - IMPORTANT: This section has the AI Generate button functionality (isGenerating state, angle generation). Move all the AI generation state and handlers from trust-and-money.tsx into this component.
    - Fields used: ct_ig_pillar*, ct_ig_p*i*, ct_ig_p*i*a* (pillar names, idea titles, angle fields).

    **storytelling.tsx (SECTION_INDEX=8) — NEW, extract from trust-and-money.tsx:**
    - Create new component with Workshop 8 content extracted from trust-and-money.tsx (roughly lines 578-849).
    - SECTION_INDEX=8, slug='storytelling'.
    - Fields used: ct_story_* keys.

    For ALL new files: follow the exact same component pattern as existing section files (default export, SectionWrapper wrapper, useAutoSave hook, same import structure). Each component must be self-contained with its own SECTION_INDEX, MODULE_SLUG, and SECTION_DEF constants.
  </action>
  <verify>
    All 11 content section files exist (overview + 10 parts + blueprint = 12 total, overview and blueprint unchanged):
    ls src/components/sections/content/ | wc -l should show 12 files.
    No TypeScript errors: npx tsc --noEmit 2>&1 | head -30
  </verify>
  <done>
    12 individual content section component files exist. sustainability.tsx has only Part 2 content, trust-and-money.tsx has only Part 6 content, content-system.tsx has only Part 5 content. Four new files (batching.tsx, starter-kit.tsx, idea-generation.tsx, storytelling.tsx) contain their respective extracted content.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update modules.ts MODULE_SECTIONS, SECTION_REGISTRY, and overview roadmap</name>
  <files>
    src/lib/modules.ts
    src/app/(app)/modules/[slug]/[section]/page.tsx
    src/components/sections/content/overview.tsx
  </files>
  <action>
    **modules.ts — Rewrite content MODULE_SECTIONS (lines 194-300) to have 12 entries:**

    The content array must become:
    ```
    0: overview          — Overview (fields: [])
    1: content-strategy  — Strategy (existing fields, unchanged)
    2: sustainability    — Sustainability (ct_sustain_* fields ONLY — remove ct_batch_* fields)
    3: batching          — Batching System (ct_batch_film_day, ct_batch_count, ct_batch_setup, ct_batch_commit)
    4: formats           — Formats (fields: [], unchanged)
    5: content-system    — Content System (fields: [], unchanged — educational only)
    6: trust-and-money   — Trust & Money (ct_tm_* fields ONLY — remove ct_ig_* and ct_story_* fields)
    7: idea-generation   — Idea Generation (ct_ig_pillar*, ct_ig_p*i*, ct_ig_p*i*a* — the pillar names, idea titles, and 80 angle fields)
    8: storytelling      — Storytelling (ct_story_* fields)
    9: starter-kit       — Starter Kit (fields: [] — educational only)
    10: cinematic-content — Cinematic Content (fields: [], unchanged)
    11: content-blueprint — Content Blueprint (existing fields, unchanged)
    ```

    Move the field definitions from the old combined entries into their correct new entries. Do NOT change any field key names — just redistribute them.

    **SECTION_REGISTRY in page.tsx — Add 4 new dynamic imports:**
    ```
    'content/batching':         dynamic(() => import('@/components/sections/content/batching'), ...)
    'content/starter-kit':      dynamic(() => import('@/components/sections/content/starter-kit'), ...)
    'content/idea-generation':  dynamic(() => import('@/components/sections/content/idea-generation'), ...)
    'content/storytelling':     dynamic(() => import('@/components/sections/content/storytelling'), ...)
    ```
    Keep all existing content entries. The registry should have 12 content entries total.

    **overview.tsx — Update roadmap slugs to point to correct new sections:**
    Change:
    - num '03' slug from 'sustainability' to 'batching'
    - num '07' slug from 'trust-and-money' to 'idea-generation'
    - num '08' slug from 'trust-and-money' to 'storytelling'
    - num '09' slug from 'content-system' to 'starter-kit'
    All other roadmap entries stay the same.
  </action>
  <verify>
    Build succeeds: npm run build 2>&1 | tail -20
    Dev server starts without errors: timeout 15 npx next dev --turbopack 2>&1 | tail -20
  </verify>
  <done>
    MODULE_SECTIONS for content has 12 entries with correct slugs, names, and field distributions. SECTION_REGISTRY has 12 content entries. Overview roadmap links point to the correct individual section slugs. Build passes.
  </done>
</task>

</tasks>

<verification>
- 12 files in src/components/sections/content/: overview, content-strategy, sustainability, batching, formats, content-system, trust-and-money, idea-generation, storytelling, starter-kit, cinematic-content, content-blueprint
- MODULE_SECTIONS['content'] has 12 entries with indexes 0-11
- SECTION_REGISTRY has 12 content/* entries
- Overview roadmap has 10 items with unique slugs pointing to correct sections
- No field key changes — existing saved data continues to load correctly
- npm run build succeeds
</verification>

<success_criteria>
Content module has 12 sections matching old HTML 1:1. Each section is its own component file, has its own route, and appears as a separate item in the sidebar navigation. Field keys are unchanged so existing user data loads correctly. Build passes without errors.
</success_criteria>
