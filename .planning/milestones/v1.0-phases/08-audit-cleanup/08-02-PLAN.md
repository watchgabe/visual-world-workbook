---
phase: 08-audit-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-deployment/07-VERIFICATION.md
autonomous: true
requirements: [DEPLOY-01, DEPLOY-03]
gap_closure: true

must_haves:
  truths:
    - "Phase 7 has a VERIFICATION.md documenting deployment readiness status"
    - "DEPLOY-01 verification distinguishes code-ready artifacts from human-action Vercel setup"
    - "DEPLOY-03 verification confirms redirect URL code is correct and documents manual Supabase step"
  artifacts:
    - path: ".planning/phases/07-deployment/07-VERIFICATION.md"
      provides: "Phase 7 deployment verification report"
      contains: "DEPLOY-01"
  key_links: []
---

<objective>
Create a VERIFICATION.md for Phase 7 (Deployment) that documents what is code-verified vs what requires human action for Vercel/Supabase configuration.

Purpose: Close the audit gap — Phase 7 is the only phase without a VERIFICATION.md. The deployment phase has human-action checkpoints (Vercel project creation, Supabase redirect URL registration) that cannot be code-verified, but the code-ready artifacts can be confirmed.
Output: `.planning/phases/07-deployment/07-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/07-deployment/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 7 VERIFICATION.md with deployment readiness evidence</name>
  <files>.planning/phases/07-deployment/07-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/07-deployment/07-01-SUMMARY.md
    - .planning/phases/07-deployment/07-01-PLAN.md
    - .planning/v1.0-MILESTONE-AUDIT.md
    - .env.example
  </read_first>
  <action>
    Create `.planning/phases/07-deployment/07-VERIFICATION.md` that verifies Phase 7 deployment readiness.

    The verification must cover these two requirements:

    **DEPLOY-01: Vercel deployment with environment variables (no hardcoded keys)**

    Code-verifiable checks (run these and record results):
    1. `grep -r "eyJ" src/` — must return 0 hits (no hardcoded Supabase JWTs)
    2. `grep -r "supabase.co" src/ --include="*.ts" --include="*.tsx"` — must return 0 hits (no hardcoded Supabase URLs)
    3. `cat .env.example` — must list all 3 required env vars: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY
    4. `grep -r "SUPABASE_SERVICE_ROLE_KEY" src/` — every hit must use `process.env.SUPABASE_SERVICE_ROLE_KEY` (server-only, no NEXT_PUBLIC_ prefix)
    5. `npm run build` — must exit 0

    Human-action status: Document that Vercel project creation and env var configuration are manual steps described in 07-01-SUMMARY.md Task 2. Mark as "human_action_required" (not a code gap).

    **DEPLOY-03: Magic link redirect URLs configured for both localhost and production**

    Code-verifiable checks (run these and record results):
    1. `grep -r "emailRedirectTo" src/` — must show LoginForm constructs redirect URL from `window.location.origin`
    2. `grep -r "auth/callback" src/app/` — must show auth callback route exists at `src/app/auth/callback/route.ts`
    3. Read the auth callback route to confirm it calls `exchangeCodeForSession` and redirects

    Human-action status: Document that adding production URL to Supabase Dashboard > Authentication > URL Configuration > Redirect URLs is a manual step. Mark as "human_action_required".

    Use this structure for the verification file:

    ```
    ---
    phase: 07-deployment
    verified: {current ISO timestamp}
    status: human_needed
    ---

    # Phase 7: Deployment — Verification

    ## Requirements

    ### DEPLOY-01: Vercel deployment with environment variables

    **Code Verification: PASSED**
    {evidence from each check}

    **Human Action: REQUIRED**
    {description of Vercel setup steps — reference 07-01-SUMMARY.md}

    **Status: SATISFIED** (code-ready; deployment is a human-action checkpoint)

    ### DEPLOY-03: Magic link redirect URLs

    **Code Verification: PASSED**
    {evidence from each check}

    **Human Action: REQUIRED**
    {description of Supabase redirect URL registration}

    **Status: SATISFIED** (code handles both localhost and production dynamically; Supabase dashboard config is manual)

    ## Summary

    | Requirement | Code Check | Human Action | Status |
    |-------------|-----------|--------------|--------|
    | DEPLOY-01   | PASSED    | Required     | SATISFIED |
    | DEPLOY-03   | PASSED    | Required     | SATISFIED |

    Phase 7 score: 2/2 requirements satisfied (code-verified; human deployment steps documented)
    ```
  </action>
  <verify>
    <automated>ls -la .planning/phases/07-deployment/07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07-deployment/07-VERIFICATION.md` exists
    - `grep "DEPLOY-01" .planning/phases/07-deployment/07-VERIFICATION.md` returns matches
    - `grep "DEPLOY-03" .planning/phases/07-deployment/07-VERIFICATION.md` returns matches
    - `grep "SATISFIED" .planning/phases/07-deployment/07-VERIFICATION.md` returns at least 2 matches
    - `grep "human_needed" .planning/phases/07-deployment/07-VERIFICATION.md` returns a match (in frontmatter status)
  </acceptance_criteria>
  <done>Phase 7 VERIFICATION.md exists with evidence-based verification of DEPLOY-01 and DEPLOY-03. Code checks documented with actual grep/build output. Human-action steps clearly distinguished from code-ready artifacts.</done>
</task>

</tasks>

<verification>
- `.planning/phases/07-deployment/07-VERIFICATION.md` exists
- Contains verification for both DEPLOY-01 and DEPLOY-03
- Distinguishes code-verified checks from human-action requirements
- References actual evidence (grep output, build results)
</verification>

<success_criteria>
Phase 7 has a VERIFICATION.md confirming deployment code readiness. DEPLOY-01 and DEPLOY-03 are verified with evidence. Human-action steps are documented but correctly identified as out-of-scope for automated verification.
</success_criteria>

<output>
After completion, create `.planning/phases/08-audit-cleanup/08-02-SUMMARY.md`
</output>
