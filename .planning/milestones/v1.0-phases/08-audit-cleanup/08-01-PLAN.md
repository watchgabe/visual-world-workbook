---
phase: 08-audit-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/workshop/SectionNav.tsx
  - src/app/api/waterfall/route.ts
  - tests/api-waterfall.test.ts
  - src/app/(app)/modules/playbook/page.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [API-03, DATA-05]
gap_closure: true

must_haves:
  truths:
    - "No orphaned SectionNav.tsx component exists in src/"
    - "No /api/waterfall route exists in src/"
    - "No unused FIELD_LABELS, HIGHLIGHT_FIELDS, SKIP_FIELDS constants in playbook/page.tsx"
    - "No eslint-disable-next-line comments suppressing unused-vars warnings for dead code"
    - "API-03 is marked as deferred to v2 in REQUIREMENTS.md"
    - "DATA-05 annotation reflects D-01 error-only design decision"
  artifacts:
    - path: "src/components/workshop/SectionNav.tsx"
      provides: "DELETED — orphaned component"
    - path: "src/app/api/waterfall/route.ts"
      provides: "DELETED — unwired route"
    - path: "tests/api-waterfall.test.ts"
      provides: "DELETED — test for removed route"
    - path: "src/app/(app)/modules/playbook/page.tsx"
      provides: "Dead constants removed, only used code remains"
  key_links: []
---

<objective>
Remove all dead code and orphaned files identified in the v1.0 milestone audit, and ensure REQUIREMENTS.md annotations are accurate for API-03 (deferred) and DATA-05 (error-only design decision).

Purpose: Close tech debt gaps so the codebase has zero orphaned components, zero dead constants, and zero unreachable routes.
Output: Cleaned codebase with no lint suppressions for dead code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned files — SectionNav, waterfall route, waterfall test</name>
  <files>src/components/workshop/SectionNav.tsx, src/app/api/waterfall/route.ts, tests/api-waterfall.test.ts</files>
  <read_first>
    - src/components/workshop/SectionNav.tsx
    - src/app/api/waterfall/route.ts
    - tests/api-waterfall.test.ts
  </read_first>
  <action>
    Delete these 3 files — they are orphaned/unwired per the milestone audit:

    1. `src/components/workshop/SectionNav.tsx` — Zero imports anywhere in src/. Created in Phase 3 (COMP-04) but replaced by SectionNavBar in Phase 5.1. SectionNavBar is the active component (imported in `src/app/(app)/modules/[slug]/[section]/page.tsx`).

    2. `src/app/api/waterfall/route.ts` — No component calls fetch('/api/waterfall'). The waterfall-analyzer feature was never fully built in the old app. Feature deferred to v2.

    3. `tests/api-waterfall.test.ts` — Test file for the removed waterfall route. No longer needed.

    After deletion, run `npm run build` to confirm no import breakage.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/components/workshop/SectionNav.tsx` returns "No such file"
    - `ls src/app/api/waterfall/route.ts` returns "No such file"
    - `ls tests/api-waterfall.test.ts` returns "No such file"
    - `grep -r "SectionNav" src/ --include="*.tsx" --include="*.ts"` returns ONLY SectionNavBar references (no SectionNav imports)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 3 orphaned files deleted. Build passes. No broken imports.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead constants from playbook/page.tsx and verify REQUIREMENTS.md annotations</name>
  <files>src/app/(app)/modules/playbook/page.tsx, .planning/REQUIREMENTS.md</files>
  <read_first>
    - src/app/(app)/modules/playbook/page.tsx
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    **Part A — Remove dead code from playbook/page.tsx:**

    Delete these unused code blocks (all have `eslint-disable-next-line @typescript-eslint/no-unused-vars` comments):

    1. Lines 9-282: The `FIELD_LABELS` constant (Record of ~270 field key to label mappings) and its preceding eslint-disable comment. This was superseded by per-module chapter renderer functions in Phase 5.

    2. Lines 284-301: The `HIGHLIGHT_FIELDS` Set and its eslint-disable comment.

    3. Lines 303-331: The `SKIP_FIELDS` Set and its eslint-disable comment.

    4. Lines 495-506 (approx): The `EmptyChapter` function component and its eslint-disable comment. Confirm it has zero references in the file before deleting.

    5. Lines 798-799 (approx — will shift after earlier deletions): The `today` variable assignment and its eslint-disable comment. Confirm it has zero references after the declaration.

    After removing these blocks, also remove any remaining `eslint-disable-next-line @typescript-eslint/no-unused-vars` comments that were guarding the deleted code. Keep any eslint-disable comments that guard USED code (like the `as any` casts).

    **Part B — Verify REQUIREMENTS.md annotations:**

    Confirm these annotations already exist (they were added during audit planning):

    1. API-03 line should contain: `DEFERRED to v2` and strikethrough text
    2. DATA-05 line should contain: `error-only by design — D-01`

    If either annotation is missing, add it. Based on current file content, both are already present — just verify and move on.

    After edits, run `npm run build` to confirm the playbook page still compiles.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FIELD_LABELS" src/app/(app)/modules/playbook/page.tsx` returns 0
    - `grep -c "HIGHLIGHT_FIELDS" src/app/(app)/modules/playbook/page.tsx` returns 0
    - `grep -c "SKIP_FIELDS" src/app/(app)/modules/playbook/page.tsx` returns 0
    - `grep -c "EmptyChapter" src/app/(app)/modules/playbook/page.tsx` returns 0
    - `grep -c "eslint-disable-next-line @typescript-eslint/no-unused-vars" src/app/(app)/modules/playbook/page.tsx` returns 0
    - `grep "API-03" .planning/REQUIREMENTS.md` contains "DEFERRED to v2"
    - `grep "DATA-05" .planning/REQUIREMENTS.md` contains "error-only by design"
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All dead constants and unused functions removed from playbook/page.tsx. Zero eslint-disable-unused-vars suppressions remain. REQUIREMENTS.md annotations confirmed for API-03 and DATA-05. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with exit 0
- No files matching `SectionNav.tsx` exist in src/components/workshop/
- No files matching `route.ts` exist in src/app/api/waterfall/
- Zero grep hits for FIELD_LABELS, HIGHLIGHT_FIELDS, SKIP_FIELDS in playbook/page.tsx
- REQUIREMENTS.md has API-03 marked deferred and DATA-05 annotated with D-01
</verification>

<success_criteria>
All orphaned files deleted, all dead code removed, all requirement annotations accurate, build passes clean.
</success_criteria>

<output>
After completion, create `.planning/phases/08-audit-cleanup/08-01-SUMMARY.md`
</output>
