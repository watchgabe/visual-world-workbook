---
phase: 05-module-migration
plan: 05
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/lib/modules.ts
  - src/components/sections/launch/overview.tsx
  - src/components/sections/launch/funnel.tsx
  - src/components/sections/launch/manychat.tsx
  - src/components/sections/launch/lead-magnet.tsx
  - src/components/sections/launch/bio.tsx
  - src/components/sections/launch/launch-content.tsx
  - src/components/sections/launch/goals.tsx
autonomous: true
requirements: [MOD-05]

must_haves:
  truths:
    - "All 7 Launch sections are accessible at their own URLs"
    - "Every form field from old launch.html exists in the new pages"
    - "Bio section has text fields for brand bio details"
    - "Goals section has goal-setting form fields"
    - "Student types in a field, reloads, and the answer is still present"
  artifacts:
    - path: "src/components/sections/launch/funnel.tsx"
      provides: "Funnel section with form fields"
      contains: "la_funnel"
    - path: "src/lib/modules.ts"
      provides: "Launch field definitions populated"
      contains: "la_"
  key_links:
    - from: "src/components/sections/launch/*.tsx"
      to: "src/hooks/useAutoSave.ts"
      via: "Workshop components with getFullResponses"
      pattern: "getFullResponses"
---

<objective>
Migrate all 7 Launch module sections from old HTML to Next.js with full content fidelity. Launch covers bio, funnel, ManyChat, lead magnet, launch content, and goals.

Purpose: Launch is a straightforward module with standard form fields. Completes the workshop-style module migrations.
Output: 7 section component files with all content and form fields from old launch.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-module-migration/05-CONTEXT.md
@.planning/phases/05-module-migration/05-RESEARCH.md
@.planning/phases/05-module-migration/05-01-SUMMARY.md

@src/hooks/useAutoSave.ts
@src/lib/modules.ts
@src/components/workshop/WorkshopTextarea.tsx
@src/components/workshop/WorkshopInput.tsx
@src/components/workshop/OptionSelector.tsx
@src/components/workshop/SectionWrapper.tsx
@old/modules/launch.html

<interfaces>
From src/hooks/useAutoSave.ts (after Plan 01):
```typescript
export interface UseAutoSaveOptions {
  moduleSlug: ModuleSlug; fieldKey: string; value: string;
  getFullResponses?: () => Record<string, string>;
}
```

From src/app/(app)/modules/[slug]/[section]/page.tsx (Plan 01):
```typescript
// Dynamic section router — imports components from src/components/sections/{slug}/{section-slug}.tsx
// Section component files must use kebab-case filenames matching section slugs
// and export default the section component
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Launch module sections (overview through lead-magnet)</name>
  <files>src/components/sections/launch/overview.tsx, src/components/sections/launch/funnel.tsx, src/components/sections/launch/manychat.tsx, src/components/sections/launch/lead-magnet.tsx, src/lib/modules.ts</files>
  <read_first>
    - old/modules/launch.html (FULL file — 951 lines. Read ALL content for sections s0 through s3.)
    - src/lib/modules.ts (MODULE_SECTIONS launch stub)
    - src/components/sections/brand-foundation/brand-journey.tsx (section pattern reference — if available from Plan 02)
  </read_first>
  <action>
**CRITICAL: Read ALL of old/modules/launch.html before writing code.** Extract every question, label, placeholder, and description for all sections.

**IMPORTANT FILE NAMING:** Use kebab-case filenames matching section slugs: `overview.tsx`, `funnel.tsx`, `manychat.tsx`, `lead-magnet.tsx`. The dynamic router imports from `@/components/sections/launch/{section-slug}`.

**1. Populate Launch field definitions in MODULE_SECTIONS:**

Fill in the `fields` arrays for ALL launch sections. Field key convention: `la_{section}_{field}`.

Example:
```typescript
'launch': [
  { slug: 'overview', name: 'Overview', fields: [] },
  { slug: 'funnel', name: 'Funnel', fields: [
    { key: 'la_funnel_awareness', required: true },
    { key: 'la_funnel_interest', required: true },
    // ... from HTML reading
  ]},
  // ... etc
]
```

**2. Create section components:**

**overview.tsx** — Static intro content from s0. No form fields.

**funnel.tsx (s1)** — `'use client'`. Funnel design questions. Follow established section pattern:
- useForm with defaultValues from SECTION_DEF.fields
- Load saved responses on mount
- SectionWrapper wrapping content
- WorkshopTextarea/WorkshopInput for fields
- Pass getValues as getFullResponses

**manychat.tsx (s2)** — `'use client'`. ManyChat & Newsletter setup questions.

**lead-magnet.tsx (s3)** — `'use client'`. Lead magnet design questions.

All content from old HTML must be migrated 1:1 per D-03.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Launch module overview through lead-magnet sections created with all content from old HTML. All form fields auto-save with full-responses pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Create Launch module sections (bio, launch-content, goals)</name>
  <files>src/components/sections/launch/bio.tsx, src/components/sections/launch/launch-content.tsx, src/components/sections/launch/goals.tsx</files>
  <read_first>
    - old/modules/launch.html (sections s4, s5, s6 — search for these section IDs to find bio, launch content, and goals sections)
    - src/components/sections/launch/funnel.tsx (pattern reference from Task 1)
    - src/lib/modules.ts (field definitions)
  </read_first>
  <action>
**Read old/modules/launch.html sections s4, s5, s6** and extract every question, description, and form field.

**IMPORTANT:** Use kebab-case filenames: `bio.tsx`, `launch-content.tsx`, `goals.tsx`.

**1. bio.tsx (s4)** — `'use client'`. Brand bio / personal bio questions. WorkshopTextarea for bio text, WorkshopInput for shorter fields. Follow established pattern.

**2. launch-content.tsx (s5)** — `'use client'`. Launch content planning questions. Standard form fields.

**3. goals.tsx (s6)** — `'use client'`. Calendar and goals section. Goal-setting fields, timeline questions. If the old HTML has date/timeline inputs, use WorkshopInput for text-based dates.

**For each:** Follow the established section pattern. Migrate ALL content 1:1.

**4. Update MODULE_SECTIONS field arrays** for bio, launch-content, goals sections if not done in Task 1.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 7 Launch module sections fully migrated. Every form field from old launch.html exists in the new pages with auto-save and response loading.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript passes
2. Navigate to `/modules/launch` — redirects to first section
3. All 7 Launch section URLs resolve correctly
4. Type in any field, blur — auto-save triggers
5. Refresh page — saved value persists
</verification>

<success_criteria>
- All 7 Launch sections accessible at unique URLs
- Every form field from old launch.html exists in new pages
- Auto-save works on all fields
- Saved responses load on mount
</success_criteria>

<output>
After completion, create `.planning/phases/05-module-migration/05-05-SUMMARY.md`
</output>
