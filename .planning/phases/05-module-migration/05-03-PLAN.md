---
phase: 05-module-migration
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/lib/modules.ts
  - src/components/sections/visual-world/Overview.tsx
  - src/components/sections/visual-world/CreatorAnalysis.tsx
  - src/components/sections/visual-world/ColorPalette.tsx
  - src/components/sections/visual-world/Typography.tsx
  - src/components/sections/visual-world/ShotSystem.tsx
  - src/components/sections/visual-world/VisualWorldDoc.tsx
  - src/app/(app)/modules/[slug]/[section]/page.tsx
autonomous: true
requirements: [MOD-03]

must_haves:
  truths:
    - "All 6 Visual World sections are accessible at their own URLs"
    - "Color palette section has hex input fields matching old HTML color picker pattern"
    - "Typography section has font name input fields"
    - "Creator analysis section has all analysis form fields from old HTML"
    - "Visual World Doc section shows compiled/summary view of prior sections"
    - "Every form field auto-saves with full-responses upsert"
  artifacts:
    - path: "src/components/sections/visual-world/ColorPalette.tsx"
      provides: "Color palette section with hex input fields"
      contains: "vw_color"
    - path: "src/components/sections/visual-world/Typography.tsx"
      provides: "Typography section with font selection fields"
      contains: "vw_typo"
    - path: "src/lib/modules.ts"
      provides: "Visual World field definitions populated"
      contains: "vw_"
  key_links:
    - from: "src/components/sections/visual-world/*.tsx"
      to: "src/hooks/useAutoSave.ts"
      via: "Workshop components with getFullResponses"
      pattern: "getFullResponses"
---

<objective>
Migrate all 6 Visual World sections from old HTML to Next.js with color palette hex inputs, typography fields, creator analysis, shot system, and compiled Visual World document view.

Purpose: Visual World is the second-largest module with specialized interactive patterns (color pickers as hex inputs, font selections). Establishes how non-standard field types are handled.
Output: 6 route-accessible section pages with all content and form fields from old visual-world.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-module-migration/05-CONTEXT.md
@.planning/phases/05-module-migration/05-RESEARCH.md
@.planning/phases/05-module-migration/05-01-SUMMARY.md

@src/hooks/useAutoSave.ts
@src/lib/modules.ts
@src/components/workshop/WorkshopTextarea.tsx
@src/components/workshop/WorkshopInput.tsx
@src/components/workshop/OptionSelector.tsx
@src/components/workshop/SectionWrapper.tsx
@src/app/(app)/modules/[slug]/[section]/page.tsx
@old/modules/visual-world.html

<interfaces>
From src/hooks/useAutoSave.ts (after Plan 01):
```typescript
export interface UseAutoSaveOptions {
  moduleSlug: ModuleSlug; fieldKey: string; value: string;
  onSaveSuccess?: () => void;
  getFullResponses?: () => Record<string, string>;
}
```

From src/components/workshop/WorkshopTextarea.tsx (after Plan 02 adds getFullResponses prop):
```typescript
interface WorkshopTextareaProps {
  moduleSlug: ModuleSlug; fieldKey: string; value: string;
  onChange: (value: string) => void; placeholder?: string;
  rows?: number; required?: boolean; label?: string;
  getFullResponses?: () => Record<string, string>;
}
```

From src/components/workshop/WorkshopInput.tsx (after Plan 02):
```typescript
interface WorkshopInputProps {
  moduleSlug: ModuleSlug; fieldKey: string; value: string;
  onChange: (value: string) => void; placeholder?: string;
  required?: boolean; label?: string;
  getFullResponses?: () => Record<string, string>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Visual World sections (overview, creator-analysis, color-palette)</name>
  <files>src/components/sections/visual-world/Overview.tsx, src/components/sections/visual-world/CreatorAnalysis.tsx, src/components/sections/visual-world/ColorPalette.tsx, src/lib/modules.ts, src/app/(app)/modules/[slug]/[section]/page.tsx</files>
  <read_first>
    - old/modules/visual-world.html (FULL file — 3,572 lines. Read ALL content for sections s0, s1, s2 to extract every question, description, and form field)
    - src/lib/modules.ts (MODULE_SECTIONS visual-world stub)
    - src/components/sections/brand-foundation/BrandJourney.tsx (section component pattern reference from Plan 02 — if available; otherwise follow pattern from Plan 02 action description)
    - src/app/(app)/modules/[slug]/[section]/page.tsx (routing entry point — add visual-world imports)
  </read_first>
  <action>
**CRITICAL: Read ALL of old/modules/visual-world.html before writing code.** Extract every question, label, placeholder, and instruction for s0, s1, s2.

**1. Populate Visual World field definitions in MODULE_SECTIONS (src/lib/modules.ts):**

Fill in the `fields` arrays for ALL 6 visual-world sections based on the old HTML. Field key convention: `vw_{section}_{field}`.

For color-palette section, the old HTML uses hex color inputs with named colors. Map these to WorkshopInput fields with appropriate field keys like:
- `vw_color_primary`, `vw_color_primary_name`
- `vw_color_secondary`, `vw_color_secondary_name`
- etc. (read HTML for exact count and names)

**2. Create section components:**

**Overview.tsx** — Static intro content from s0. No form fields. Can be a server component.

**CreatorAnalysis.tsx (s1)** — `'use client'`. Analysis questions about creator style, audience, content patterns. Follow the same pattern as Brand Foundation sections:
- useForm with defaultValues from SECTION_DEF.fields
- Load saved responses on mount via Supabase query
- SectionWrapper wrapping all content
- WorkshopTextarea/WorkshopInput for each form field
- Pass `getValues` as `getFullResponses` to all workshop components

**ColorPalette.tsx (s2)** — `'use client'`. This is the most specialized section:
- The old HTML has color inputs (hex values) with color name labels
- Use WorkshopInput for hex values (e.g., `placeholder="#F0601B"`)
- Add a small color swatch preview next to each input using inline style: `<div style={{ width: 24, height: 24, borderRadius: 4, background: watch('vw_color_primary') || '#ccc' }} />`
- This is a simple 1:1 migration — the old HTML used `<input type="text">` for hex values, NOT `<input type="color">`. Match this approach.
- Mood board references: If the old HTML has "mood board" as image URL inputs, use WorkshopInput for URLs. If it references file uploads, simplify to URL inputs (per Research: "mood board uses WorkshopInput for image URLs")

**3. Add visual-world section imports** to the `[slug]/[section]/page.tsx` routing file.

All instructional text, headings, and descriptions from old HTML must appear 1:1 in JSX.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/visual-world/Overview.tsx exists
    - src/components/sections/visual-world/CreatorAnalysis.tsx exists and contains `'use client'`
    - src/components/sections/visual-world/ColorPalette.tsx exists and contains `'use client'`
    - src/lib/modules.ts visual-world sections have fields with keys starting with `vw_`
    - ColorPalette.tsx contains hex-related placeholder text (e.g., `#` or `hex`)
    - All section components contain `MODULE_SLUG = 'visual-world'`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Visual World overview, creator analysis, and color palette sections are migrated with all content from old HTML. Color palette has hex inputs with color swatch previews.</done>
</task>

<task type="auto">
  <name>Task 2: Create Visual World sections (typography, shot-system, visual-world-doc)</name>
  <files>src/components/sections/visual-world/Typography.tsx, src/components/sections/visual-world/ShotSystem.tsx, src/components/sections/visual-world/VisualWorldDoc.tsx</files>
  <read_first>
    - old/modules/visual-world.html (sections s3, s4, s5 — search for these section IDs to find typography, shot system, and compiled visual world doc content)
    - src/components/sections/visual-world/CreatorAnalysis.tsx (pattern reference from Task 1)
    - src/lib/modules.ts (field definitions from Task 1)
  </read_first>
  <action>
**Read old/modules/visual-world.html sections s3, s4, s5** and extract every question, description, and form field.

**1. Typography.tsx (s3):**
- `'use client'`. Font selection fields — the old HTML likely has inputs for heading font, body font, accent font, etc.
- Use WorkshopInput for font name fields (e.g., `vw_typo_heading_font`, `vw_typo_body_font`)
- Follow established section pattern: useForm, load on mount, SectionWrapper, getFullResponses

**2. ShotSystem.tsx (s4):**
- `'use client'`. Visual shot/style inputs — the old HTML has image style descriptions, shot angles, composition preferences
- Use WorkshopTextarea for style descriptions, WorkshopInput for shorter values
- Field keys: `vw_shot_*`

**3. VisualWorldDoc.tsx (s5):**
- `'use client'`. This is a compiled view of all Visual World responses, similar to a mini-playbook for this module.
- Read the old HTML s5 to understand what it displays
- If it's read-only (displays values from s1-s4), fetch all visual-world responses on mount and display them formatted
- If it has additional form fields, include those too
- Use the playbook-style card layout: `background: 'var(--card)'; border: '1px solid var(--border)'; borderRadius: 'var(--radius-lg)'; padding: '14px 16px'`
- Display field values with `|| 'Not yet completed'` fallback for empty values

**4. Update routing** in `[slug]/[section]/page.tsx` to include these 3 sections.

All content from old HTML must be migrated 1:1.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/visual-world/Typography.tsx exists and contains `'use client'`
    - src/components/sections/visual-world/ShotSystem.tsx exists and contains `'use client'`
    - src/components/sections/visual-world/VisualWorldDoc.tsx exists and contains `'use client'`
    - Typography.tsx contains font-related field keys (e.g., `vw_typo`)
    - ShotSystem.tsx contains shot-related field keys (e.g., `vw_shot`)
    - All three files contain `MODULE_SLUG = 'visual-world'`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All 6 Visual World sections are fully migrated. Typography has font selection fields, shot system has style description fields, and Visual World Doc shows a compiled view of all section responses.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript passes
2. Navigate to `/modules/visual-world` — redirects to first section
3. All 6 Visual World section URLs resolve correctly
4. Color palette section shows hex inputs with color swatches
5. Typography section shows font input fields
6. Auto-save works on all fields
</verification>

<success_criteria>
- All 6 Visual World sections accessible at unique URLs
- Color palette has hex inputs with visual preview swatches
- Typography has font name input fields
- Every form field from old visual-world.html exists in new pages
- Visual World Doc shows compiled view of responses
</success_criteria>

<output>
After completion, create `.planning/phases/05-module-migration/05-03-SUMMARY.md`
</output>
